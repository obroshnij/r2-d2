<label>Domains count</label>
<div class='panel whois-count'>
  Completed: <%= @completed.length %> out of <%= @pending.length + @completed.length %><br>
  Sucessful: <%= @completed.successful().length %><br>
  Failed: <%= @completed.failed().length %><br><br>
  <%= _.map(@completed.failed(), (whois) -> whois.get('name')).join(', ') %><br><br>
  <% if @completed.failed().length > 0 and @pending.length == 0 : %>
    <a class='retry-failed'>Retry failed</a>
  <% end %>
</div>
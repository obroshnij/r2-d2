<label>Name
  <%= f.text_field :name %>
</label>
 
<label>Email Address
  <%= f.text_field :email %>
</label>
 
<% if can? :read, Role %>
  <%= f.label :role_id, "Role" %>
  <%= f.collection_select :role_id, current_user.accessible_roles, :id, :name %>
<% end %>
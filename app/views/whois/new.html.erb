<% provide(:title, "Whois") %>

<h1>Whois</h1>
<div class="row">
  <div class="small-4 small-offset-4 columns">
    <%= form_tag(controller: "whois", action: "create") do %>
      <div class="row collapse">
        <div class="small-10 columns">
          <%= text_field_tag :name %>
        </div>
        <div class="small-2 columns">
          <%= submit_tag "Go", class: "button postfix" %>
        </div>
      </div>
    <% end %>
  </div>
</div>

<% if @record %>
  <row>
    <div class="small-6 large-offset-3 panel whois-box">
      <% @record.each_line do |line| %>
        <%= line %> <br>
      <% end %>
    </div>
  </row>
<% else %>
  <row>
    <div class="large-6 large-offset-3 panel callout">
      <p>I can look up domains, TLDs and IP addresses</p>
    </div>
  </row>
<% end %>